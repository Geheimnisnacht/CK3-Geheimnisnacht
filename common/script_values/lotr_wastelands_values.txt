@elven_slow_construction_time_base = 8760
elven_slow_construction_time = @[elven_slow_construction_time_base] # Ported from LOTR (00_building_values.txt); used by dead_marshes_cleared - name is a LOTR leftover but the value itself is generic

lotr_WAC_gold_limit_value = {
	value = 0

	if = {
		limit = { 
			any_owned_story = {
				story_type = story_cycle_automated_colonisation_settings
				has_variable = lotr_WAC_gold_setting
				save_temporary_scope_as = WAC_settings
			}
		}
		add = scope:WAC_settings.var:lotr_WAC_gold_setting
	}
}